version: "3.9"

services:
  app:
    build: .
    ports:
      - "8085:8085"
    env_file:
      - .env
    depends_on:
      - postgres

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: reminder
      POSTGRES_USER: reminder
      POSTGRES_PASSWORD: r2emind3r
